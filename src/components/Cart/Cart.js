import React from "react";
import "./Cart.css";
// import { myntra_log } from "../../images";

// import { CartContext } from "../../context/cartContext";

function Cart(props) {
    // let [cartItem, setCartItem] = React.useContext(CartContext);

    return (
        <div className="cart-wrapper">
            <button >Cart</button>
        </div>
    );
}

export default Cart;
